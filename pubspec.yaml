name: encrypt_decrypt
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  encrypt: ^5.0.3
  google_fonts: ^6.3.2
  msix: ^3.16.12

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

msix_config:
  display_name: Saya Encrypt Decrypt
  publisher_display_name: Maxlink
  publisher: CN=Maxlink
  identity_name: com.maxlink.encryptdecrypt
  msix_version: 1.0.0.0
  logo_path: windows/runner/resources/app_icon.ico
  capabilities: internetClient
  certificate_path: C:\Windows\System32\SayaExportCertificate.pfx
  certificate_password: Saya@1234
